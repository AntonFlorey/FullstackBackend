{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "fullstack part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo $0",
    "build:ui": "rm -rf build && cd ../FullStack/Part2/phonebook2 && npm run build && cp -r build ../../../FullstackBackend",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy"
  },
  "author": "Anton F",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
